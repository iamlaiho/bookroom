<nav class="navbar navbar-default">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data toggle="collapse" data-target="navbar" aria-expanded="false" aria-controls="navbar">
			<span class="sr-only">Toggle</span>
			<span class="icon-bar"></span>
			</button>
			<div class="navbar-brand"><%= link_to "Starfall", bookings_path(id: @user) %></div>
		</div>
		<div id="navbar" class="collapse navbar-collapse">
			<ul class="nav navbar-nav navbar-right">
				<li class="dropdown">
					<button class="dropbtn"><%= @user.name %></button>
					<div class="dropdown-content">
						<%= link_to "Sign out", home_path %>
					</div>
				</li>
			</ul>
		</div>
	</div>
</nav>


<div class="container">
	<%= form_tag new_booking_path, method: :get do %>
		<% flash.each do |field, msg| %>
			<div class="alert alert-danger" role="alert">
				<%= msg %>
			</div>
		<% end %>

		<div class="form-inline">
			<%= text_field_tag :start_date, "", class: 'form-control', placeholder: 'YYYY-MM-DD' %>
			<%= submit_tag "Search", class: 'btn btn-primary' %>
		</div>
	<% end %>

	<h3>Booking History</h3>
	<table class="table">
		<thead>
			<tr>
				<th>Room Number</th>
				<th>Date</th>
				<th>Start Time</th>
				<th>End Time</th>
			</tr>
		</thead>
		<tbody>
			<% @booked_slots.sort_by { |e| [e.start_date, e.room_id]}.each do |booking|%>
				<tr>
					<td><%= booking.room.number %></td>
					<td><%= booking.start_date.strftime("%Y-%m-%d") %></td>
					<td><%= booking.start_date.strftime("%H:%M") %></td>
					<td><%= booking.end_date.strftime("%H:%M")  %></td>
				</tr>
			<% end %>
		</tbody>
	</table>
</div>
